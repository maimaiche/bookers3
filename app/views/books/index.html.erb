
 <p>User info<%= image_tag @user.get_profile_image(100,100) %></p>
 <% if @user == current_user %>
  <p><%= link_to "プロフィール編集", edit_user_path(@user) %></p>
  <% end %>

<h1>New book</h1>
  <%= form_with model: @book do |f| %>
    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Opinion</h4>
      <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
  <% end %>

  <% @books.each do |book| %>
<div>
<%= link_to book_path(book.id) do %>

  <% end %>
 <p>Title : <%= book.title %></p>
  <p>Opinion：<%=book.body %></p>
  <p>User_id：<%=book.user.name %></p>
  <p>destroy : <% if @book.user == current_user %>
    <%= link_to "destroy", book_path(@book), method: :delete %>
  <% end %>
  </p>
</div>
<% end %>

